# 프로젝트 허브 — DESIGN.md

> 작성일: 2026-08-19 · PRD.md의 디자인 상세 문서
> 이 문서는 "어떻게 보이고 어떻게 움직이는가"만 다룬다. 기능과 화면 구성은 PRD.md 참조.

---

## 0. 한 문단 요약

늦은 밤 작업실. 방은 완전한 검정이 아니라 **살짝 갈색기가 도는 따뜻한 어둠**이고, 책상 위 스탠드 하나가 켜져 있다. 그 빛 아래 프로젝트 화면들이 살아 있는 채로 놓여 있다. 손을 가까이 가져가면 그 물건 가장자리로 램프 빛이 번진다.

허브 자체는 색을 거의 쓰지 않는다. **색은 프로젝트 화면에서만 나온다.**

---

## 1. 무드 — 심야 작업실

### 정한 것
- 배경은 순수한 검정(#000)이 **아니다**. 따뜻한 기운이 도는 어둠
- 텍스트는 순백(#FFF)이 **아니다**. 따뜻한 크림색
- 조명은 하나. 램프 색 앰버

### 왜
순수 검정 + 순백 조합은 대비가 극단적이라 눈이 빨리 피로하고, 인상이 차갑고 기계적이다. 이 사이트는 개인 작업물을 모아둔 곳이라 사람 손길이 느껴지는 편이 맞다. 갈색기를 아주 조금 섞는 것만으로 인상이 "실험실"에서 "작업실"로 바뀐다.

### 피해야 할 인상
차가운 파란빛 어둠(SF·기업용 대시보드 느낌), 완전한 무채색 검정(고급 브랜드 사이트 느낌). 둘 다 이 사이트의 성격이 아니다.

---

## 2. 컬러

### 팔레트

| 이름 | HEX | 쓰이는 곳 |
|---|---|---|
| `ink` | `#16130F` | 페이지 배경 |
| `surface` | `#1F1A15` | 카드 바탕 |
| `line` | `#2B2420` | 테두리, 구분선 |
| `cream` | `#F2EBE2` | 제목, 주요 텍스트 |
| `muted` | `#A89C90` | 설명문, 보조 텍스트 |
| `amber` | `#E9A94C` | 강조 — 버튼, 현재 위치, 빛 번짐 |
| `amber-dim` | `#7A5C2C` | 비활성 상태, 약한 테두리 |

```css
:root {
  --ink:       #16130F;
  --surface:   #1F1A15;
  --line:      #2B2420;
  --cream:     #F2EBE2;
  --muted:     #A89C90;
  --amber:     #E9A94C;
  --amber-dim: #7A5C2C;
  --glow:      rgba(233, 169, 76, 0.18);
}
```

### 색 사용 규칙

1. **강조색은 앰버 하나뿐이다.** 초록·빨강 같은 상태 색도 추가하지 않는다. 오류나 실패는 색이 아니라 문구로 알린다
2. **앰버는 아껴 쓴다.** 한 화면에 앰버가 칠해진 영역이 세 곳을 넘지 않게 한다. 흔해지면 강조 기능을 잃는다
3. **앰버 위의 글자는 `--ink`.** 어두운 글자를 얹는다. 밝은 글자는 대비가 부족하다
4. **`--muted`보다 어두운 텍스트 색은 쓰지 않는다.** 어두운 배경에서 회색을 더 낮추면 읽을 수 없다

### 왜 강조색이 하나인가
이 허브에 실리는 프로젝트들이 이미 각자의 색을 갖고 있다. 맛집은 코랄, 날씨는 시간에 따라 변하는 하늘색. 카드 안에서 이 색들이 동시에 보이는데 허브까지 색을 여러 개 쓰면 화면이 정리되지 않는다. 허브는 액자여야지 그림이 되면 안 된다.

---

## 3. 타이포

### 글꼴
**Pretendard 하나.** 제목과 본문 모두 같은 글꼴을 쓴다.

```css
font-family: 'Pretendard Variable', Pretendard, -apple-system, sans-serif;
```

Pretendard는 Variable 버전을 쓴다. 굵기를 100~900 사이에서 자유롭게 조절할 수 있어, 글꼴이 하나여도 표현 폭이 넓다.

### ⚠️ 글꼴이 하나일 때의 함정
제목과 본문이 같은 글꼴이면, 차이를 만들 수단이 **굵기·크기·자간**밖에 없다. 이 셋의 차이를 충분히 벌리지 않으면 화면 전체가 평평해지고 "성의 없어 보이는" 인상이 된다.

그래서 아래 수치는 일부러 대비를 크게 잡았다. **임의로 좁히지 말 것.**

### 타입 스케일

| 역할 | 크기 | 굵기 | 자간 | 행간 |
|---|---|---|---|---|
| 랜딩 타이틀 | `clamp(2.75rem, 7vw, 5.5rem)` | 800 | `-0.035em` | 1.05 |
| 프로젝트 이름 | `1.625rem` | 700 | `-0.02em` | 1.2 |
| 랜딩 소개문 | `1.0625rem` | 400 | `0` | 1.75 |
| 한 줄 설명 | `0.9375rem` | 400 | `0` | 1.6 |
| 라벨 · 숫자 | `0.75rem` | 500 | `+0.14em` | 1 |

### 규칙
- **큰 글씨일수록 자간을 좁힌다.** 제목은 음수 자간(`-0.035em`), 작은 라벨은 양수 자간(`+0.14em`). 크기가 커질수록 글자 사이가 벌어져 보이는 착시를 보정하는 것
- **굵기는 400과 700/800만 쓴다.** 500·600을 섞으면 차이가 애매해져 오히려 지저분해진다
- **라벨의 넓은 자간은 영문·숫자에만.** 한글에 자간을 벌리면 단어가 분해돼 보인다. `3 projects` 같은 표기에만 적용
- **한 화면에 크기 종류 4개 이하.** 그 이상은 위계가 아니라 소음

---

## 4. 형태 · 간격

### 모서리
| 요소 | 값 |
|---|---|
| 카드 | `12px` |
| 미리보기 영역 | `8px` |
| 버튼 | `8px` |
| 인디케이터 점 | 원형 |

완전한 각(0px)은 딱딱하고, 알약 모양(999px)은 가볍다. 12px은 손으로 만든 물건 같은 인상을 준다.

### 간격 (4px 기준)
`4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96 · 128`

이 값들만 쓴다. 17px, 23px 같은 임의의 값을 쓰기 시작하면 화면 전체의 리듬이 무너진다.

### 테두리
- 기본: `1px solid var(--line)`
- 어두운 배경에서 카드 경계를 만드는 건 그림자가 아니라 이 얇은 선이다. 그림자는 어둠 속에서 보이지 않는다

---

## 5. 인터랙션

### 5.1 시그니처 — 앰버 빛 번짐

**이 사이트에서 가장 기억에 남아야 할 순간.** 카드에 마우스를 올리면 가장자리에서 램프색 빛이 새어 나온다.

```css
.card {
  border: 1px solid var(--line);
  transition: border-color 260ms var(--ease),
              box-shadow   260ms var(--ease);
}

.card:hover,
.card:focus-visible {
  border-color: var(--amber-dim);
  box-shadow: 0 0 0 1px var(--amber-dim),
              0 12px 48px -8px var(--glow);
}
```

**규칙**
- 빛은 **번지기만 한다.** 카드가 커지거나 위로 뜨거나 기울지 않는다
- 밝기 변화만으로 반응을 만든다. 위치·크기는 고정
- 키보드로 이동했을 때(`:focus-visible`)도 똑같이 빛나야 한다. 마우스 사용자만 반응을 보는 건 안 된다

### 5.2 움직임 기본값

```css
--ease: cubic-bezier(0.22, 0.61, 0.36, 1);
--fast: 150ms;   /* 색·투명도 */
--base: 260ms;   /* 빛 번짐, 상태 변화 */
--slow: 420ms;   /* 페이지 진입 */
```

- 튕기거나(bounce) 목표를 지나쳤다 돌아오는(overshoot) 움직임은 쓰지 않는다
- 무한 반복 애니메이션은 쓰지 않는다. 유일한 예외는 미리보기 로딩 중 표시
- 한 번에 움직이는 요소는 최대 3개

### 5.3 화면별 움직임

**랜딩 진입**
타이틀 → 소개문 → 버튼 순으로 `80ms` 간격, 각각 `420ms` 페이드인 + 아래에서 `8px` 상승. 딱 한 번만 재생하고 끝.

**트랙 스크롤**
`scroll-snap-type: x mandatory` · `scroll-behavior: smooth`. 카드가 화면 중앙에 오면 미리보기 로드 시작.

**최초 진입 힌트**
`0.75s` 후 "옆으로 스크롤" 문구가 나타나고, 사용자가 한 번이라도 스크롤하면 즉시 사라진다. 스크롤이 없어도 `5s` 후 자동으로 사라진다.

### 5.4 reduced motion

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
  html { scroll-behavior: auto; }
}
```

색과 빛 변화 자체는 유지한다. 시간만 0으로 만든다. 반응이 아예 사라지면 클릭 가능한지 알 수 없어진다.

---

## 6. 상태 디자인

미리보기는 실패할 수 있는 기능이라, 실패한 모습까지 디자인해야 한다.

| 상태 | 화면 |
|---|---|
| 로딩 전 | 스크린샷을 `blur(12px)` + 밝기 60%로 표시 |
| 로딩 중 | 그 위에 앰버 가로선이 좌→우로 천천히 이동 (유일하게 허용되는 반복 애니메이션) |
| 로딩 완료 | `--slow` 동안 블러가 풀리며 실제 화면으로 교체 |
| 실패 | 스크린샷을 선명하게 표시 + 하단에 `미리보기를 불러오지 못했습니다` (`--muted`, 라벨 크기) |
| 미리보기 없음 | `--surface` 바탕에 프로젝트 이름만. 앰버 사용 안 함 |

**실패를 빨간색으로 표시하지 않는다.** 사용자 잘못이 아니고 사이트는 여전히 정상 작동한다. 조용히 알리고 넘어간다.

---

## 7. 문구 톤

- **평서문, 문장 부호 최소.** "프로젝트를 둘러보세요" (O) / "프로젝트를 둘러보세요!" (X)
- **버튼은 눌렀을 때 일어나는 일을 그대로 적는다.** `들어가기` (O) / `Enter` `시작` (X)
- **한 요소는 한 가지 일만 한다.** 프로젝트 이름은 이름만, 설명은 설명만. 설명에 "클릭하세요"를 넣지 않는다
- **실패 문구는 사과하지 않는다.** 무슨 일이 있었는지만 알린다

---

## 8. 금지사항

디자인이 흔들릴 때 돌아올 기준. 아래는 **하지 않는다.**

### 8.1 움직임
- ❌ 스크롤에 따라 요소가 튀어 오르거나 흔들리는 효과
- ❌ 시차 스크롤(패럴랙스)
- ❌ 카드가 마우스를 따라 기울어지는 3D 효과
- ❌ 무한 반복 애니메이션 (로딩 표시 제외)
- ❌ 페이지 진입 애니메이션의 2회 이상 재생

> **왜**: 카드 하나하나가 이미 살아 움직이는 실제 웹사이트다. 그 위에 움직임을 더하면 무엇을 봐야 할지 알 수 없어진다. 이 사이트의 움직임 예산은 이미 미리보기가 다 쓰고 있다.

### 8.2 색과 빛
- ❌ 그라데이션 배경 (특히 보라-파랑, 앰버-핑크)
- ❌ 형광색 · 네온 발광
- ❌ 앰버 외의 강조색 추가
- ❌ 여러 색이 섞인 빛 번짐

> **왜**: 어두운 배경에서 가장 빠지기 쉬운 함정이고, 넣는 순간 흔한 템플릿처럼 보인다. 그리고 프로젝트 화면의 실제 색과 충돌한다.

### 8.3 요소
- ❌ 이모지 (🚀 ✨ 💡 등)
- ❌ 일러스트 아이콘, 3D 그래픽
- ❌ 아이콘 세트 도입 (필요하면 최소한의 화살표만 CSS로)
- ❌ 배경 패턴, 노이즈 텍스처, 격자 오버레이

> **왜**: 톤이 가벼워지고, 프로젝트 화면의 시각적 무게와 어울리지 않는다.

### 8.4 타이포
- ❌ 두 번째 글꼴 도입
- ❌ 한글에 넓은 자간 적용
- ❌ 굵기 500·600 사용
- ❌ 텍스트에 그림자나 발광 효과

---

## 9. 허용 목록

금지사항만 있으면 위축되니, 명시적으로 괜찮은 것도 적어둔다.

- ✅ 반투명 유리 효과 — 단, **iframe 위에는 덮지 않는 조건**으로. 상단 바나 힌트 문구 배경 정도는 가능
- ✅ 앰버 빛 번짐의 강도 조절 — 취향에 따라 `--glow` 투명도를 0.1~0.3 사이에서 조정
- ✅ 카드 폭·간격 조정 — 실제로 띄워보고 답답하거나 허전하면 바꿀 것
- ✅ 다크 모드 전용 유지 — 라이트 모드는 만들지 않는다. 이 사이트의 성격상 불필요

---

## 10. 접근성 최소 기준

- 키보드 `Tab`으로 모든 카드에 접근 가능, 포커스 위치가 눈에 보일 것
- 텍스트 대비: `--cream` / `--ink` = 약 14:1, `--muted` / `--ink` = 약 7:1 — 둘 다 기준 통과
- 앰버 버튼 위 어두운 글자 대비 확인 필수
- iframe에 `title` 속성으로 프로젝트 이름 제공
- 미리보기 로딩 상태를 스크린 리더에 알림 (`aria-busy`)

---

## 11. 남은 결정

1. 랜딩 소개문 실제 문구
2. 카드 폭 최종 수치 (뷰포트 65% / 70% / 75% 중 실측 후 결정)
3. 인디케이터 형태 — 점 / 가는 진행 바 / 숫자(`01 / 03`)
4. 트랙 마지막 "돌아가기" 영역의 생김새
