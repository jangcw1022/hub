# 프로젝트 허브 — PRD

> 작성일: 2026-08-19 · 버전: v1 초안

---

## 1. 개요

내가 만든 웹 프로젝트들을 한곳에 모아 보여주는 개인 포트폴리오 허브.
각 프로젝트의 **실제 화면이 살아 움직이는 상태로** 카드에 담기고, 카드를 누르면 해당 프로젝트로 이동한다.

정적인 스크린샷 나열이 아니라 "지금 돌아가고 있는 사이트들의 진열장"이라는 점이 이 사이트의 핵심 성격이다.

---

## 2. 목표 / 비목표

### 목표
- 프로젝트를 한 화면에서 훑고, 마음에 드는 걸 눌러 바로 들어갈 수 있게 한다
- 각 프로젝트가 **어떻게 생겼는지**를 설명 없이 즉시 전달한다
- 프로젝트가 추가돼도 파일 한 곳만 고치면 되게 한다

### 비목표 (v1에서 하지 않는 것)
- 프로젝트 상세 소개 페이지 (허브는 관문 역할만, 설명은 각 프로젝트가 직접)
- 로그인 / 관리자 화면 / DB
- 블로그, 이력서, 연락처 폼
- 검색·필터·정렬

---

## 3. 기술 스택 & 배포

| 항목 | 결정 |
|---|---|
| 프레임워크 | Next.js (App Router) |
| 스타일 | Tailwind CSS |
| 배포 | Vercel — **신규 레포 / 신규 프로젝트** |
| 데이터 | 코드 내 배열 (`src/data/projects.ts`) |
| 백엔드 | 없음 (전면 정적) |

**신규 레포로 분리한 이유**: 허브가 독립적이라 프로젝트를 추가·제거해도 기존 배포에 영향이 없고, 나중에 커스텀 도메인을 허브에 물리기 쉽다. 대신 각 프로젝트는 **배포된 절대 URL**로 연결한다.

---

## 4. 정보 구조

```
/            랜딩 — 타이틀 + 소개 + 입장
/projects    프로젝트 트랙 — 가로 스크롤 카드
```

프로젝트 카드 클릭 → **외부 URL 새 탭**으로 이동 (`target="_blank" rel="noopener"`).
같은 탭으로 나가면 허브로 돌아오기가 번거로워 새 탭을 기본으로 한다.

---

## 5. 화면별 요구사항

### 5.1 랜딩 (`/`)

풀스크린 한 화면. 스크롤 없음.

**구성 요소**
- 타이틀: "프로젝트 허브"
- 소개문: 2~3줄. 이 사이트가 무엇인지, 무엇을 볼 수 있는지
- 입장 버튼 → `/projects`

**동작**
- 진입 시 타이틀 → 소개문 → 버튼 순으로 짧게 순차 페이드인
- 버튼 외에 Enter 키로도 입장 가능
- 하단에 프로젝트 개수를 조용히 표시 (예: `3 projects`)

### 5.2 프로젝트 트랙 (`/projects`)

가로로 흐르는 트랙. 카드가 오른쪽으로 이어진다.

**레이아웃**
- 카드 폭: 뷰포트의 약 65~70%, 좌우에 다음 카드가 살짝 걸쳐 보이게
- `scroll-snap-type: x mandatory` — 카드 단위로 딱 걸림
- 카드 간격 넉넉히, 트랙 양끝 여백 확보

**카드 내용**
1. 라이브 미리보기 (카드 면적의 대부분)
2. 프로젝트 이름
3. 한 줄 설명

> 기술 스택·GitHub 링크·제작 시기는 v1에서 화면에 노출하지 않되, 데이터 구조에는 선택 필드로 자리를 만들어 둔다.

**입력 처리**
| 입력 | 동작 |
|---|---|
| 마우스 휠 (세로) | 가로 이동으로 매핑 |
| 트랙패드 가로 스와이프 | 네이티브 |
| ← → 방향키 | 이전 / 다음 카드로 스냅 |
| 드래그 | 좌우 드래그로 이동 |
| 카드 클릭 | 해당 프로젝트 새 탭 |

**위치 안내**
- 하단에 현재 위치 인디케이터 (점 또는 진행 바)
- 최초 진입 시 "옆으로 스크롤하세요" 힌트를 잠깐 노출 후 자동 소멸
- 마지막 카드 뒤에 짧은 마감 영역 (여백 + "돌아가기")

---

## 6. 데이터 모델

`src/data/projects.ts`

```ts
export type Project = {
  id: string;            // 고유 키
  name: string;          // 화면 표시명
  tagline: string;       // 한 줄 설명
  url: string;           // 배포된 절대 URL
  fallbackImage: string; // iframe 실패 시 대체 스크린샷 (/public)
  // v2 예정 (선택)
  stack?: string[];
  repoUrl?: string;
  year?: string;
};
```

**v1 등록 대상**

| id | 이름 | 한 줄 설명 (초안) |
|---|---|---|
| `naver-news` | 네이버 뉴스 검색 | 키워드로 뉴스를 모아 보는 검색 인터페이스 |
| `weather` | 날씨 | 도시를 검색하면 하늘빛이 바뀌는 7일 예보 |
| `matzip` | 맛집 | 지도 위에 나만의 맛집을 기록하는 관리 도구 |

*이름과 설명은 확정 전 초안. URL은 실제 배포 주소로 채워 넣을 것.*

---

## 7. 라이브 미리보기 — 상세 사양

이 기능이 사이트의 핵심이자 최대 리스크 요인이므로 별도로 정의한다.

### 7.1 렌더링 방식
- `<iframe>`을 **데스크톱 폭(1280×800)으로 렌더링**한 뒤 CSS `transform: scale()`로 카드 크기에 맞춰 축소
- 그냥 작은 iframe을 쓰면 프로젝트의 모바일 레이아웃이 잡혀 의도한 화면이 안 나온다
- `transform-origin: top left`, 부모에 `overflow: hidden`

### 7.2 로딩 전략
- `IntersectionObserver`로 **화면에 들어올 때만** iframe 마운트
- 인접 카드 1개까지는 미리 로드 (스크롤 시 빈 화면 방지)
- 화면에서 완전히 벗어난 카드는 언마운트해 메모리 회수
- 로드 전에는 `fallbackImage`를 블러 처리해 표시 → 로드 완료 시 부드럽게 교체

### 7.3 상호작용 차단
- iframe 위에 투명 오버레이를 덮어 내부 클릭·스크롤을 막는다
- 카드 클릭 이벤트는 오버레이가 받는다
- `sandbox="allow-scripts allow-same-origin"` 적용

### 7.4 실패 대응
| 상황 | 처리 |
|---|---|
| `X-Frame-Options` / CSP로 차단 | `fallbackImage` 정적 표시 |
| 로드 타임아웃 (5초) | `fallbackImage`로 전환 |
| URL 자체 오류 | 카드에 "미리보기 없음" 상태 표시 |

> **선행 확인 필요**: 세 프로젝트가 실제로 iframe 삽입을 허용하는지 구현 전에 테스트한다. Vercel에 올린 Next.js 기본 설정은 프레임 차단 헤더를 넣지 않으므로 통과할 가능성이 높지만, 확인 없이 진행하지 않는다.

### 7.5 스크린샷 fallback 준비
- 모든 프로젝트에 대해 1280×800 캡처를 미리 확보해 `/public/previews/`에 둔다
- iframe이 정상 동작하더라도 fallback은 반드시 존재해야 한다

---

## 8. 반응형

| 구간 | 동작 |
|---|---|
| 데스크톱 (≥1024px) | 가로 트랙, 라이브 iframe |
| 태블릿 (768~1023px) | 가로 트랙 유지, 카드 폭 확대 |
| 모바일 (<768px) | **세로 스크롤로 전환**, iframe 대신 정적 스크린샷 |

**모바일에서 세로로 바꾸는 이유**: 가로 스와이프는 세로 스크롤 습관과 충돌해 조작이 헷갈리고, 모바일에서 여러 사이트를 iframe으로 띄우면 성능 부담이 크다.

**`prefers-reduced-motion`**: 켜져 있으면 페이드인·자동 애니메이션을 끄고 스냅만 유지한다.

---

## 9. 디자인 방향 — 다크 갤러리

DESIGN.md에서 확정하되, 방향만 요약한다.

- 어두운 배경 위에 프로젝트 화면이 **빛나듯 떠 있는** 구성
- 화면 자체가 광원이므로 카드 테두리·그림자는 최소화하고, 은은한 발광(glow)으로 존재감을 준다
- 허브의 색은 거의 무채색으로 억제 — 색은 프로젝트 화면에서만 나온다
- 타이포: Pretendard (한글 안정적)
- 텍스트는 배경보다 살짝 밝은 회색 계열, 순백은 타이틀에만

---

## 10. 범위 정리

### v1 (지금 만드는 것)
- 랜딩 + 가로 트랙 2개 화면
- 프로젝트 3개 등록
- 라이브 iframe + fallback
- 반응형, 키보드 조작

### v2 이후 후보
- 기술 스택 태그 / GitHub 링크 노출
- 프로젝트 상세 페이지
- 카테고리 필터
- 프로젝트 데이터 Supabase 이관 + 등록 화면
- OG 이미지 자동 생성

---

## 11. 미결 사항

1. 랜딩 소개문 실제 문구
2. 각 프로젝트의 표시명과 한 줄 설명 확정
3. 세 프로젝트의 배포 URL
4. iframe 삽입 허용 여부 실측
5. 사이트 제목 (브라우저 탭에 뜰 문구) — "프로젝트 허브" 그대로 갈지
